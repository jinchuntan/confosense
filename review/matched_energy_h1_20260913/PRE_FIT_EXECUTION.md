# Matched energy unit: execution freeze

14 September 2026 local time. This execution is authorized by the user's handoff at `f44d4d1bd784254237bd643b9389fc9ce8c380a3`, read with `git show`. Implementation continues from `f1a71a19b4e227a9894a8e2c74686a642dca962a`; the older instruction branch was not checked out. The original no-fitting proposal remains immutable.

Exactly one real matrix unit is authorized: PLEIA-energy horizon 1, outer fold 0, model seed 42; persistence, XGBoost and Attention-LSTM, each with its own fixed 90%/95% absolute-error split-conformal intervals. The data and role hashes match the proposal, with 29,719 fitting, 9,907 calibration and 9,909 test rows. Eight tuning and two final learned fits are planned; no model fit is shared across different model families, and both levels share their own fitted model.

The new modules implement generic identities, whole-run chronological roles, freezing, readiness, run, independent validation and explicit resume. Original pilot modules and their scope guards remain unchanged. Final fitted artifacts, calibration/test and tuning predictions, histories, actual parameters, fit membership and phase resources are checkpointed atomically per model. The validator independently reconstructs ranks, errors, bounds, coverage, width, Winkler, candidate/epoch choices and saved-model predictions. Current-source resume rechecks fresh data; it does not impersonate historical source hashes.

Eight focused checks passed on the final implementation. Two small synthetic smoke versions completed ten learned fits each (20 total, separate from real fitting). The second rerun followed freeze/membership integrity changes; both attempts and exact source identities are preserved. An intentional interruption before the first learned smoke fit verified persistence checkpoint reuse. Saved models reproduced all smoke calibration/test predictions exactly, and completed resume made zero fits. Corrupt checkpoints and changed source, configuration and data were rejected.

All 39 published role banks / 273 roles passed the no-fit compatibility check, including 12 RICO horizon/fold banks. The first checker incorrectly required exported source-row order to equal role iteration order. BDG2 `inner1_train` concatenates purged blocks; its exported membership file retains source-row order. The corrected check verifies identical membership sets plus the already published **ordered** role hashes. No memberships, algorithms, model data or historical results changed. The failed no-fit command and successful corrected command are retained. Read-only validation also confirmed the old pilot's nine point/eighteen interval cells with zero refits.

CPU execution, numerical/Torch threads 1, `n_jobs=1`, training and inference batch 256, fixed candidates, training-only normalization, early stopping and interval ranks remain unchanged. The subsequent user instruction asked to continue without another launch-memory stop. The new authorization therefore retains 3 GiB as a recorded reference rather than a blocking threshold; the readiness check actually exceeded it. The existing 256 MiB epoch floor and 8 GiB disk check remain. This changes no model, target, candidate, threshold or completed scientific result.

The pre-fit commit includes the frozen protocol, readiness, tests, no-fit compatibility, this manifest and exact evaluated source archive. The run environment records the actual commit and raw source hash. No remaining queue unit, conditional challenge, full method grid or robustness run is authorized here. Full-study readiness remains false.
