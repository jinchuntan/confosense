# Attempts and supersession

The first preparation invocation stopped before writing evidence because the frozen virtual environment does not contain the optional `psutil` package. No package was installed. The process inventory was changed to a read-only Windows query limited to Python processes, and preparation then completed.

The first focused run passed all 12 bounded tests. Before publication, the adapter was strengthened to verify the delegated v1 source against its audited Git blob while allowing CRLF checkout normalization. The first successful results in `focused_tests/` and the first `PREFLIGHT_READINESS.json` are retained as provisional evidence. `focused_tests_v2/` and `PREFLIGHT_READINESS_V2.json` reran the same suite under the source-identity gate and are authoritative.

No model fit, calibration, experiment, full scientific validation, or five-seed analysis occurred in either attempt.

The first package-verification receipt marked only its own link missing because the verifier checked links before writing itself; its other 13 checks passed. `PACKAGE_VERIFICATION.json` is preserved. `PACKAGE_VERIFICATION_V2.json` treats its own future output path as expected and is authoritative.
