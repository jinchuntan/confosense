# Complete PLEIA-energy fold-2 conditional-context model seeds 43–46

## Recommended model and outcome

Use **GPT-5.6 Terra / High**. Sol / High is also suitable if accessible. The scientific protocol and real seed-42 path are implemented and validated; this task adds a bounded coordinator, runs the remaining seeds and performs the already specified aggregation. Escalate only for a concrete unresolved scientific issue, not because the numerical run lasts many hours.

Start from **review/pleia-energy-context005-pilot-20260915**, commit **35ae9cc7f2809d1bb2dce19d4d9b6a65490100da**. Preserve valid local descendants. Retrieve only this handoff from its instruction branch; do not check out or merge that branch's older experiment code.

The user authorizes the necessary bounded coordinator/analysis implementation, targeted tests, all four real units, independent validation, completed resumes, five-seed analysis, backup and publication. Proceed automatically through all stages without another fitting approval.

Create a new non-main review branch, suggested review/pleia-energy-context005-multiseed-20260915. Preserve main, seed-42 results, prior review heads, historical untracked files and external backups.

## Read and retain the current contract

Read these pinned seed-42 records:
- review/pleia_energy_context005_pilot_20260915/NEXT_BOUNDED_PROPOSAL.md
- PLEIA_ENERGY_CONTEXT005_PILOT_REPORT.md and EVIDENCE_INDEX.md in that directory.
- The exact seed-42 execution manifest, frozen protocol, readiness, operation reconciliation, independent validation and zero-fit resume.
- The implementation coordinator/recovery helpers and the current src.conditional_context005 entrypoint.
- src.context005_metrics and its inference acceptance tests.
- AMENDMENT005_PROPOSAL.md for equal-context weighting and original-context inference.

The current CLI manifest validator already accepts model seeds 42–46. Do not rewrite or loosen its scientific guard. Most new work belongs in a versioned batch coordinator and analysis directory. Keep scientific source unchanged where possible; any actual repair must have a specific regression test and explicit source/output impact.

## Exact four-unit execution scope

All four units retain:
- pleia_energy, outer fold 2, h1 / ten minutes, confidence .95.
- Original 14,855 fitting rows, 4,954 calibration rows, 23 features and 9,907 held-out clean rows.
- The identical 68 original contexts, 2,856 published fault slots and 68 clean identities.
- The identical seven families, three severities, two slot seeds 42/43, signs -/+, onsets, masks, null/alias definitions and effective support.
- Four controls: quantile_static, cqr_static, cqr_rolling, persistence_static.
- Five rules: single_sample, 30min_3of3, 60min_4of6, 180min_3of18, 360min_4of36.
- Rolling CQR every 24 observations/window 250/minimum 50; immutable native-static and online-score conventions.
- Original 68.79861111111111 asset-days, including all 115 eligible tail readings.
- Numerical-only, availability-only and combined channels.
- No selection and no energy-sensitivity mask.

Only the **model seed** changes, sequentially to **43, 44, 45 and 46**. Fault-slot seeds do not change. Fresh quantile owners must receive the actual model seed; seed-specific filenames are insufficient.

Expected new operations are four CQR wrappers containing **12 learned HistGradientBoosting quantile-estimator fits**, four logical conformalizations (eight nested profiler calls), and four deterministic persistence-radius calculations. Zero XGBoost, LSTM, EnbPI, DSCP, tuning or matched-forecast fits. Share raw/CQR/rolling owners within a seed, not learned fits across seeds. Existing within-context alias reuse remains intact.

Each seed has 171,360 event/control/rule/channel records and 60 canonical macro cells. Across four new units this is **685,440 records and 240 seed-specific macro cells**. With seed 42, there are 300 seed-specific macro cells and 60 five-seed aggregate comparisons. The independent original contexts remain **68**, not 340.

The seed-42 realization/alias structure is a fixed input property; verify the same context features, observations and availability across seeds. Do not require differing learned predictions merely because seeds differ. Identical numerical results are possible; independently confirm the actual estimator random_state/seed parameters instead.

## Implement, freeze and test before fitting

Build a durable sequential four-unit coordinator with unique execution manifests, protocol/run/validation/resume paths and a batch journal. Suggested run keys are pleia_energy_f2_s43_C_v1 through pleia_energy_f2_s46_C_v1, after confirming they are unused. Never overwrite an existing valid run.

Freeze the entire batch scope and exact commands before launching the first fit. Bind each seed's manifest, source/packages, role/feature/context/schedule hashes and allowed operation counts. Preserve the seed-42 protocol and validation receipts; read it as historical evidence.

Use the existing CLI actions and explicit_real_C_run authorization kind. New manifests must be execution-authorized by this handoff; proposal-only artifacts remain unchanged.

Targeted preflight checks must demonstrate:
- Exactly seeds 43–46, unique outputs and seed propagation to all three native quantile estimators.
- No mutation of slot RNGs, memberships, input features, calibration policy or endpoint settings.
- Manifest/owner mismatches and corruption rejected.
- Imported coordinator helpers resolve before starting a worker.
- Correct active-worker adoption, completed-stage reuse and no duplicate fits after interruption.
- Aggregate keys reject duplicates, missing seeds or mismatched context/stratum support.
- Frozen inference resamples original contexts in chronological adjacent blocks, with every paired control/stratum carried together.

Use stubs/no-fit tests where sufficient. If tiny fitting is genuinely necessary for a new regression, keep it bounded, explicitly synthetic and separately logged. Do not repeat the entire historical synthetic workload or broad test suite for unchanged code. Satisfy applicable required gates and tests for actual changes.

## Sequential run, validation and recovery

For each seed, finish freeze/readiness/run/independent validate/completed resume before starting the next. Record actual command, PID/process identity, start/end, exit code, scope and resource consumption.

Every seed must independently reconstruct fault algebra, causal features, saved-owner predictions, native and rolling bounds, delayed scores, alerts, onset matching, context/stratum aggregation and original workload. Preserve the existing numerical tolerances and available/unavailable rules. Do not reduce validation because it takes longer than fitting.

Completed resume must invoke zero learned/calibrator fits and zero replay updates while preserving all scientific files. Preserve failure records and safely adopt a surviving worker. Never blindly repeat a completed or incomplete owner fit. Bounded infrastructure or reader repairs may proceed with focused tests and explicit provenance; do not change scientific settings to recover.

Use current CPU/single-thread policy, sequential workers, nonblocking 3-GiB RAM reference and 8-GiB disk floor. Check sufficient room for raw evidence, temporary archives, indexes and backups before the batch. Do not redesign scientific storage or delete raw evidence just to meet an estimate.

Measured seed-42 planning baseline:
- Four runs: approximately 3.50 hours.
- Four independent validations: approximately 5.93 hours.
- Four completed resumes: approximately 0.41 hours.
- Raw new output: approximately 2.12 GiB, before packaging/index/backup overhead, with potentially over half a million small files.

Thus roughly 9.8 hours of measured-work scaling precedes other overhead. This is not a deadline. Preserve durable restart information if a session or machine interruption prevents completion; do not promise a live process will survive an unknown environment shutdown.

## Five-seed aggregation and inference

Reuse the unchanged seed-42 result at its pinned source/protocol identity; do not rerun it. Put all combined analysis outside every completed scientific run.

Reconcile every expected dataset/fold/control/rule/channel/context/family/severity key against the published effective/eligible schedule. Require exactly one contribution per required model-seed/key after the established slot averaging. Missing rows, duplicate seed contributions or inconsistent null masks must not silently change denominators.

Follow the frozen order:
1. Average effective slot detections within each original context/stratum/model seed.
2. Average the five model-seed contributions within that same context/stratum.
3. Average original contexts equally within each stratum.
4. Average all 21 supported strata equally.

All five model seeds must be present; do not select favorable seeds. Repeated deterministic persistence outputs do not create independent training or data replicates.

Run the existing PLEIA original-context inference using seed 20240601, 2,000 draws, seven adjacent daily contexts within original segments/folds, fixed original-length/tail handling and existing support/nondegeneracy gates. Use chronological onset order, not opaque row hashes. Retain unavailable intervals for unsupported or degenerate cases.

Independently reconstruct the seed pooling, draw-index structure, per-stratum means and percentile endpoints. The two existing inference tests are useful but are not a substitute for checking real contribution-key completeness. All controls use the same paired context draws. Reuse the seed-42 report's fixed control-contrast set if reporting paired detection differences; do not select contrasts from the new results.

Label any resulting intervals as **approximate original-context block-bootstrap uncertainty conditional on this historical setting and the declared protocol**. Five seeds alone do not establish population generalization. Keep per-seed variability separate from uncertainty due to the observed contexts.

The current inference routine covers detection. Do not invent workload or delay confidence intervals by reusing fault-context draws that omit the original full-stream tails. Report those outcomes descriptively unless an already specified, independently verified original-exposure procedure exists. No new significance/equivalence or deployment-feasibility claims.

Keep all 20 control/rule combinations and all three channels visible. Report detection jointly with full-stream episodes/day, time in alert, restricted TTD (110-minute convention), misses and conditional interval diagnostics. A low episode count with long time in alert must remain visible.

## Evidence, publication and completion

Deliver:
- Batch manifests, exact commands and durable progress/restart records.
- Four run/validation/zero-fit resume receipts and actual operation/resource ledgers.
- All seed-specific summaries and original-context contributions.
- A five-seed comparison report/CSV, inference support/status tables, shared draw identities, paired contrasts where supported, and independently verified aggregate metrics.
- Compact figures with source hashes, the updated panel response, CURRENT_EVIDENCE.md and remaining-work status.
- Full scientific evidence packaged through the existing lossless archive convention, with member paths/bytes/hashes and restoration instructions.

Keep headline CSVs, protocols, ownership summaries and validation records directly accessible on GitHub. Archive the many raw files in suitably sized parts, verify every new archive member and remote part hash, and retain scientific source files locally. Publish incrementally by completed validated seed where useful; retain one final batch index. Do not download/revalidate every unchanged historical archive.

Verify changed remote identities and representative exact downloads; complete the existing external-backup procedure without overwriting earlier verified bundles. Confirm main and earlier review heads are unchanged.

Upon success, record five completed C model-seed units for PLEIA-energy fold 2/h1. Other matrices remain **70/195 matched forecasting, 250/1950 interval quality and 9/27 unique seasonal computations**. This batch does not complete other C datasets/folds, the full BDG2 operational grid, energy sensitivity or legacy robustness/recovery.

Finish all four authorized units, not just the first one. Return the published branch/SHA, verified counts, actual costs, five-seed findings and inference limits. Propose the next concrete dependency from the agreed remaining implementation/execution plan without automatically launching an unrelated queue. If genuinely blocked, report exact stage/seed, error, active/completed work and restart command.
