# Durable command receipts

These are exact copies of the external durable-logger outputs and terminal receipts for the final pre-fit regression, four-protocol freeze, real-data coordinator, no-fit aggregation, delivery acceptance and reporting seal. Per-seed worker commands, logs and actual exits are in the published coordinator `attempts/` tree.
