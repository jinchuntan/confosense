# Execute the first real PLEIA-energy conditional-context pilot

## Model and authorized outcome

Recommended assistant: **GPT-5.6 Terra / High**. Sol / High is also suitable if available. This is execution of an implemented, synthetically validated protocol. Astra / Extra High is reserved for a concrete unresolved scientific/causality problem; ordinary execution, reporting and bounded debugging should proceed without a model-change approval loop.

Continue from review/context-replay005-implementation-20260914 at **6e57bf4350012e3265ea2416f2e6b0bfcc12f8c3**, preserving valid local descendants.

The user authorizes the exact PLEIA-energy fold-2/model-seed-42/h1 real C pilot below: activation, fresh freeze/readiness, fitting, all context replays, independent validation, completed resume, analysis, backup and publication. This is the new execution instruction required by the published activation helper. Do not ask again for fitting permission or stop after readiness.

Retrieve only this handoff document from its instruction branch. Retain current experiment code; do not merge the instruction branch's older code.

Create a new non-main review branch, suggested review/pleia-energy-context005-pilot-20260914. Preserve main, all prior review heads, historical results, existing untracked historical files and backups.

## Read the exact published proposal and current implementation

At the starting commit, read:
- review/context_replay005_implementation_20260914/FIRST_CONDITIONAL_CONTEXT_RUN_PROPOSAL.md
- first_real_run_proposal_manifest.json
- FIRST_RUN_NO_FIT_READINESS.json
- first_real_coordinator.py and its common/coordinator helpers
- IMPLEMENTATION_REPORT.md, EVIDENCE_INDEX.md, and PROTOCOL_TO_CODE_CROSSWALK.md
- The current src.conditional_context005 entrypoint and its execution/source/recovery guards.
- The existing proposal-v3 freeze and readiness, exact context/schedule/zero-control files, and owner-role compatibility table.

Use the existing tested protocol and executable commands. No new method, data-cleaning rule, model selection or experimental redesign belongs to this task.

## Fixed real-data scope

- Dataset: pleia_energy, original meter values.
- Outer fold: 2.
- Model seed: 42.
- Primary horizon: 1 step, ten minutes.
- Confidence level: .95.
- Endpoint: C_effective_fault_conditional_context; fixed evaluation with no inner or outer challenge-score selection.
- All **68 published outer contexts**, all **2,856 scheduled fault slots** (seven families x three severities x two slots per context), and **68 clean context identities**.
- Slot seeds 42/43 and signs -/+ retain their published role; they are not extra model seeds.
- Four controls: quantile_static, cqr_static, cqr_rolling, persistence_static.
- Five rules: single_sample, 30min_3of3, 60min_4of6, 180min_3of18, 360min_4of36.
- Rolling CQR: every 24 observations, window 250, minimum score pool 50.
- All numerical-only, availability-only and combined event/alert channels.
- Preserve fixed nulls, deterministic aliases, onsets, masks, scopes and support rules.

Expected canonical event/control/rule/channel records: **171,360**, retaining null rows before aggregation. These records are not independent observations.

Original full-stream clean workload uses **9,907 rows**, **68.79861111111111 asset-days**, including **115 eligible readings outside the 68 challenge tiles**. Count that original exposure once for each control/rule/channel; aliases and fault variants must not multiply it.

Training: **14,855 rows**, calibration: **4,954 rows**, **23 features**. The published matched-forecast owner has different fitting/calibration memberships and must not be reused.

Expected new computation:
- One HistGradientBoosting CQR wrapper, three quantile-estimator fits with the frozen max_iter=300 settings.
- One public logical conformalization; two nested instrumented calls are not two independent calibrations.
- One persistence absolute-error radius computation, zero learned persistence fits.
- No XGBoost, Attention-LSTM, EnbPI, DSCP, tuning or matched-forecast refits.
- Raw/static-CQR/rolling controls share the legitimate quantile owner and compatible raw predictions. Rules consume shared streams without causing new predictor fits.

Record actual operations and investigate any unexpected extra fitting. Existing regression/debug fits remain separate.

## Activation and execution

First inspect branch/working tree, active processes, existing execution manifests, output paths, disk and recovery state. Reconcile any existing run rather than launching a duplicate. Verify current source, dependencies, prepared-cache/input hashes and proposal identities.

The recorded readiness source SHA-256 is:
83243b4bfb70f56831c7e092fed2a120f67bffc11880d54665c11607c4087841

Confirm against the current authoritative source manifest. Do not bypass a mismatch. A justified implementation repair requires focused tests, clear source lineage and an appropriate new freeze; preserve prior evidence.

Invoke the existing helper from the repository root, using the actual existing Python environment:

```powershell
& C:/cfs_venv/Scripts/python.exe -B review/context_replay005_implementation_20260914/first_real_coordinator.py --acknowledge-new-user-authorization
```

The helper creates a separate authorized execution manifest and distinct protocol/output paths:
- protocols/conditional_context005/pleia_energy_f2_s42_C_v1
- outputs/conditional_context005/pleia_energy_f2_s42_C_v1
- outputs/conditional_context005/first_real_C_v1_coordinator

These are relative to smart_building_conformal. Leave proposal-only artifacts inert and unchanged. The coordinator's CLI workers must use their required smart_building_conformal working directory.

Proceed through freeze, readiness, run, independent validate and completed resume automatically. Retain durable progress, actual worker identity/command/exit, atomic checkpoints and failed attempt records. A background launch or notification is not completion evidence.

Preserve the current CPU/thread policy, sequential workers, nonblocking 3-GiB RAM reference and 8-GiB disk floor. The proposal's synthetic extrapolation is approximately 50.5 minutes execution plus 59.8 minutes validation before real fitting and other overhead; it is not a guaranteed deadline. Measure actual costs. Do not reduce contexts, controls, rules or validation to meet that estimate.

On interruption, adopt/reconcile a surviving worker, reuse valid completed stages and never blindly repeat an incomplete or completed fit. Correct bounded infrastructure/reader problems while preserving scientific results and documenting compatibility. If a genuine scientific defect blocks execution, retain the evidence and explain its exact scope rather than altering thresholds or suppressing checks.

## Scientific invariants and acceptance

Preserve the implemented causal chain: corruption at observation time enters subsequent actual features and serialized-owner predictions. Context-specific adaptive state, delayed releases, missingness handling and group isolation remain intact. Never substitute saved clean predictions for a corrupted variant.

Require:
1. All five CLI stages and final coordinator have actual successful exits.
2. Exact context, schedule, control, rule, channel and membership counts reconcile with the manifest.
3. Independent reconstruction passes for fault realization, causal features, direct-estimator predictions, native bounds, score release, rolling updates, alerts, episode matching, equal-context aggregation and clean exposure.
4. All 68 zero controls and declared aliases pass their identity requirements; invalid/null slots are retained with reasons and correct positive denominators.
5. Completed resume performs zero learned fits, zero calibrator fits and zero replay/calibration updates while preserving all scientific run files.
6. Shared-owner and issued/consumed stream hashes, operation counts and measured costs reconcile.
7. Unsupported stratum/full-macro/inference outputs remain explicitly unavailable. Do not silently average only supported strata.
8. Actual source/config/data identities and the unchanged historical evidence are recorded.

Do not rerun the entire 446-test suite or all synthetic fits merely for an unchanged-code execution. Run required gates and targeted tests for any actual repair; reuse the published acceptance receipts when their hashes match.

## Analysis and report

Produce a pilot report, exact CSVs, a compact comparison figure, evidence index, measured cost table and remaining-work/restart status.

The main descriptive comparison has **20 fixed control/rule combinations**. Present:
- Conditional-context macro detection, with per-stratum and effective-context denominators.
- Numerical-only, availability-only and combined channels separately.
- Full-stream clean background episodes per asset-day and time in alert.
- Miss counts, detected-only delays and restricted time-to-detection/censoring.
- Clean versus corrupted interval diagnostics with clearly named truth/observation definitions.
- Scheduled/effective/null/alias counts and original-unit contributions.
- Shared-owner computation, actual fitting/replay/validation/resume wall/CPU and peak memory.

Preserve the implemented timestamp convention (PLEIA restriction endpoint 110 elapsed minutes); do not silently change it to 120. Explain the six-observation envelope plus matching-tolerance convention in the report where needed.

No policy is selected by these outer results. Describe paired control differences without claiming equivalence, significance or a best deployment configuration. This is one model seed on one historically inspected evaluation period. Do not fabricate five-seed or population uncertainty intervals.

C is detection conditional on declared synthetic faults and contexts. It does not establish real-world fault prevalence, deployment precision/F1, or amendment-004 primary operational feasibility. Keep its endpoint and evidence separate from BDG2's natural-frequency benchmark and the matched 1,950-cell interval matrix.

The unapproved energy sensitivity proposal is independent and **does not block this primary run**. Do not adopt, apply or search that mask in this task. Preserve primary meter values and all original workload denominators.

## Publication and finish

Publish code changes only if actually required, the authorized execution manifest, frozen protocol, fitted artifacts, results, streams or documented lossless parts, all validation records, costs, report and index on the new non-main review branch. Follow existing data-publication conventions.

Update CURRENT_EVIDENCE.md and the panel/readiness status with one completed C pilot if acceptance passes. Matched forecasting stays 70/195, interval quality stays 250/1950 and unique seasonal stays 9/27; C does not increment those matrices.

Verify changed remote file identities and representative exact downloads including headline metrics, protocol, issued/consumed evidence and validation records. Reuse historical manifests instead of downloading 11,824 unchanged artifacts again. Complete the existing external-backup procedure and confirm main/prior review heads remain unchanged.

Return the published branch URL/SHA, evaluated source hash, verified counts, actual operations/costs, control/rule findings and genuine limitations. End with one concrete next bounded proposal based on the measured costs and fixed remaining scope, without launching a wider queue.

Do not stop at activation, a running worker or "ready to validate" when the authorized workflow can finish. If blocked, provide the precise error, affected stage, active/completed worker status, preserved paths and exact restart command.
