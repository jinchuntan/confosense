# Reporting-only corrections after the evaluated pre-fit commit

The evidence index's initial recomputation example placed its output outside the repository. `analyze.reports` and figure provenance use `out.relative_to(ROOT)`, so that example would fail when creating source links. The publisher now gives an absolute path beneath the repository, resolved from the repository working directory, and accurately states that saved execution manifests retain the original absolute layout.

This changes only the generated documentation string in `publish.py`. No scientific source, fitted object, candidate, interval, metric, frozen manifest, training command or running worker changed. The evaluated scientific digest remains `cd907183301a189ddfcc195774dad58c8fa07d5b326d2ac29cf3636383dc681e`. The original evaluated helper identity remains in `evaluated_commit.json`; later publication commits preserve this documentation correction separately in their history.

After all 52 units completed, `delivery.py` packaged actual outer-process exit receipts and `final_checks.py` checked final arithmetic and figure-source identities without fitting. Their successful durable command receipts are in `delivery_v1`. The supplemental Panel-2 CSV includes both nominal levels; it does not replace the original paired contrasts.

Visual inspection found overlapping horizon text in the original cost plot. A second layout moved these labels into corner legends, which obscured some points. The final `cost_figure.py` layout uses a shared central legend and was visually checked at full resolution: all four dataset panels, horizon markers and individual-seed/mean points are readable. The other original figures were also inspected. All original figures and the intermediate layout remain preserved; the final cost PNG/PDF and source hashes are in `delivery_v1/cost_figure_v3`. No numerical value or scientific output was changed.

`delivery_v1/COMPLETE.json` binds the initial receipt package at its creation time. Later reporting checks, figure variants and publication receipts have their own source/validation records and are covered by the final repository-wide task `EVIDENCE_MANIFEST.csv`; the initial completion file is not claimed to cover subsequently added files.
