# Overnight batch pre-fit record

Starting experiment code: d95405a005851dd7ffc3635a51034cb883b98671. Authorizing handoff: a53725c77e36547a978e5b8d46be422ad837139f, review/NEXT_TASK_MATCHED_OVERNIGHT_BATCH.md. The older instruction branch was read with git show; its implementation was never checked out or merged.

All twelve keys match the previously published queue in order. Fresh freeze and no-fit readiness passed for every key, including both inner roles. Each protocol binds current scientific source, authorization, configuration, package, matrix, membership, boundary, data and schema identities. Exact planned run/validate/resume arrays are in the joint manifest; actual attempts record absolute paths and receipts separately. There have been zero learned fits during preparation.

Scientific source SHA-256 remains cd907183301a189ddfcc195774dad58c8fa07d5b326d2ac29cf3636383dc681e. Only review orchestration, analysis/reporting and its tests were added. The unchanged source bytes are archived. The evaluated commit is recorded separately before launching the detached coordinator.

Twenty-four distinct checks passed: fifteen existing scientific integrity tests and nine dummy-command orchestration tests. The coordinator checks cover exact order, completed restart without new commands, failing validation stopping progression, no automatic failed retry, exclusive lock, PID reuse rejection, partial checkpoint resume choice, adopting a live logger/actual receipt, and missing exits remaining unknown. The orchestration subset was rerun after final lifecycle changes. No tiny learned fits were repeated.

Preserved implementation attempts: the first preparation helper reached successful unit-1 freeze/readiness, then rejected its in-memory tuple keys against the runner's list-based authorization check. Reading the unchanged saved JSON fixed that helper; completed checks were retained. Its initial parent exit was not captured by the first launcher, so it is not invented; the traceback proves failure and the corrected retry has a durable exit-0 receipt. The first test collection failed because psutil was absent; standard-library Windows APIs replaced that dependency without installing packages. A subsequent lock test exposed a locked-file read error; initialization now checks file size without reading the locked byte. Both failed test logs and the successful runs remain in overnight_batch_v1.

The coordinator uses one OS file lock outside OneDrive, PID/creation/executable identities (including venv shim descendants), immutable command logs and actual exit receipts, an atomic progress ledger and append-only attempts. It executes each run, independent artifact/metric validation, forbidden-fit resume, then saved-stream group reconciliation before advancing. It automatically aggregates, renders figures, writes reports, commits, backs up and attempts publication after the last unit. Publication-only authentication failure is recorded without blocking the science queue.

The nonblocking 3 GiB launch reference, 256 MiB epoch floor, 8 GiB disk check, CPU-only one-thread execution, n_jobs=1 and lazy batch256 are unchanged. No retuning, clipping, extra seeds, thresholds, challenge or broader experiment is authorized by this batch.
