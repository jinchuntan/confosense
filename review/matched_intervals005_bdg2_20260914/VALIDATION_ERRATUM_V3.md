# EnbPI accumulator precision: second validation correction

The pilot's evaluated outputs remain unchanged. The second validator passed the repaired native-CQR checks, then exited **1** while independently reconstructing EnbPI out-of-bag scores. Maximum discrepancy was 0.0001220703125 kWh. The coordinator halted and preserved the attempt.

The native MAPIE calibration routine allocates its out-of-fold prediction matrix with `dtype=float` (float64), then inserts XGBoost's float32 predictions before averaging. The independent validator initially averaged the float32 array directly. Correcting the independent accumulator to float64 reconstructs **all three saved OOB score arrays exactly**, maximum discrepancy **0**, including the same nonfinite-score positions. See [the three-horizon diagnosis](enbpi_precision_diagnosis.csv). No fitting occurred and no tolerance was relaxed.

The evaluated scientific source remains `026da0df7a10396c455a6a8d28a2736532e9a439a61e38ddea04d830bd142f19`. The final corrected validator source is `d663b3c7ab0debfa06ae3bdccd536244bb221eb302d141ffa6c93eef18d99e8a`. [The revision-3 audit manifest](VALIDATION_ERRATUM_V3.json) pins both source trees and preserves the same strict, completed-read-only compatibility route introduced in [the CQR erratum](VALIDATION_ERRATUM.md). The [resolved CQR/native method specification](method_specification_resolved_v2.json) is unchanged by this precision correction.

Nine no-fit regression checks passed on the final source: the preceding eight source/native-CQR checks plus a new float64 OOB reconstruction check using both saved synthetic EnbPI owners. Thus **47 distinct acceptance checks are resolved**. These reruns performed zero learned-estimator and zero calibrator fits. The original synthetic records, two failed independent validators, preceding source archive and prior erratum are preserved.

[Version-3 recovery commands](VALIDATION_RECOVERY_COMMANDS_V3.json) write `validation_v3` and `validate_worker_resources_v3.json`, leaving both earlier attempts intact. A completed resume retains `--forbid-fits` and the explicit revision-3 audit manifest. [The corrected source archive](corrected_validation_source_v3.zip) makes this validation version reproducible. The final report and worker-cost CSV include both failed-validation costs separately.

The standalone precision diagnosis initially ran from the repository directory, causing the existing loader to create three duplicate BDG2 input files there. All three were verified byte-identical to the existing original inputs and moved to the named external backup directory; no original data was moved or deleted. [The preservation receipt](diagnostic_cache_preservation.json) records paths and hashes. The diagnostic's working directory is now explicitly the experiment project directory.
