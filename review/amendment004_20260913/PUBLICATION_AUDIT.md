# Amendment 004 publication audit

Authorized scope: the user's current implementation, amendment/configuration, four-task no-fitting support, bounded synthetic smoke, verification logs and report, published on the non-main descendant `review/amendment004-20260913`. Entry commit is `3354393290d2afaef4361a9a7a1a43336a14c21d`; evaluated source is `917d6eb2d634a457f3fba0fc16133463fe05b9b4`. There is no history reset, force push or main merge.

[EVIDENCE_MANIFEST.csv](EVIDENCE_MANIFEST.csv) records the changed publication files relative to the entry commit, their Git blob IDs, byte sizes, SHA-256 and purpose. Hashes describe **staged Git blob bytes**, so they also describe the published objects despite Windows line-ending conversion. The manifest and its generated verification JSON exclude themselves to avoid recursive hashes. The earlier root review manifest is a historical record for commit 3354393 and remains unchanged.

Publication includes derived no-fit row/group/time memberships, exposure, event schedules, training-only scale metadata, rank/rule support, and synthetic interval/alert outputs. Raw/interim/processed datasets, local prepared pickle caches, virtual environments, external backups and credentials are excluded. The largest new artifacts are compressed BDG2 memberships and the calibration-rank table; each is below GitHub's per-file limit. The failed first smoke, intermediate failed regression and evaluated source archives remain preserved, explicitly separate from final successful evidence.

The final report, index and protocol link the exact source/configuration identities and command logs. The [preservation audit](../../smart_building_conformal/outputs/amendment004/validation/preservation.json) verifies all 125 original pilot/config/protocol files and 554 older data/output files byte-for-byte; 19 historical report annotations were already present at entry and remain unchanged. The prior review bundle hash also matches. A new external Git bundle will preserve this publication without replacing an existing backup.

[Publication validation](publication_validation.json) checks the allowlist, manifest objects, byte counts, key-pattern scan, internal links, source archives, configuration identity and completed checkpoint hashes before push. It verifies published artifact integrity, not scientific validity. Branch and commit publication are verified against origin after push; the final delivery gives the exact remote SHA.
