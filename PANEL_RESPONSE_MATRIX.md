# ConfoSense current panel response matrix

13 September 2026, after the completed three-fold BDG2 reduced-grid operational benchmark. **0/3 units selected a primary configuration; 3/3 abstained under the unchanged confidence-bound gates.** All three original commands exited 0; both new units passed saved-stream metric reconstruction and zero-fit completed resume. The checkpoint repair preserved the prior fold-2 result. The original forecasting pilot, historical results, main and backups remain preserved. See the [completed benchmark](BDG2_THREEFOLD_BENCHMARK_REPORT.md), [memory repair](CHECKPOINT_MEMORY_REPAIR_REPORT.md) and [current evidence index](review/CURRENT_EVIDENCE.md). Full-study readiness remains **false**.

| Panel topic | Completed evidence | Still required |
|---|---|---|
| Explain conformal prediction | Own-model split-conformal forecasting pilot plus CQR operational intervals compared with the shared uncalibrated quantile fit; empirical coverage, width and Winkler95 retained. | Concise final presentation of calibration, temporal-dependence limits and prediction intervals versus metric confidence intervals. |
| Dataset sources, purpose and selection | Four-task structural preflight; all ten retained BDG2 buildings, actual temporal memberships and source/derived-data attribution published. | Complete broader source-purpose-selection bibliography and justify operational support treatment for each task. |
| Separate datasets or merge | BDG2 benchmark pools only commensurate hourly kWh metrics across its own periods; three source datasets/four tasks remain separate. | Broader matched task/horizon evaluation without pooling raw errors across physical units. |
| Justify methods | Nine frozen operational candidates, CQR-owned HistGradientBoosting quantiles, shared uncalibrated controls and zero-fit persistence; actual estimator ownership recorded. | Full method/candidate scope, including remaining DSCP/seasonal questions; this is not the point XGBoost/Attention-LSTM comparison. |
| Explain train/calibration/test protocol | Three BDG2 historical outer periods, two expanding purged inner folds, reserved final calibration, 39 unchanged role hashes and 18 validated boundaries. | Complete broader declared folds/model seeds and explain prior inspection and repeated buildings in the dissertation. |
| Explain k-of-m rules | Immediate, 180-minute 3-of-3 and 360-minute 4-of-6 frozen candidates; causal replay, confidence-bound gates, abstention, clean workload and censored detection accounting verified. | Justified handling of unsupported tasks; no threshold or incidence changes simply to produce feasibility. |
| Simplify presentation | Source-linked per-fold and pooled reports, exact settings, conditional paired recall/workload contrasts and all channel/stratum CSVs. | Revised final slides/manuscript with clear limitations; no blanket same-recall superiority or prospective-generalization claim. |
| Verify methodology and ablation | Both new 18-cell/90-pair inner matrices and derived outer matrices pass independent reconstruction; shared controls, inverse objective, persistence, original-building pairing and zero-fit resume are retained. | Full-grid/method and broader robustness scope, justified unsupported-task design, and inference at the actual study scope. |
| Is LSTM's extra cost worthwhile? | Preserved PLEIA point/interval/computational pilot and its prior diagnostics; the current operational model is explicitly distinct. | Matched point/interval/compute evidence across all 13 declared task/horizon combinations and broader folds/seeds. No requirement that LSTM win. |

The next bounded step is the no-fitting support-design audit for PLEIA temperature, PLEIA energy and RICO. They are retained in scope; their current inner banks are unsupported at the unchanged incidence. No full-study, extra-seed or additional dataset run is authorized by this delivery.

## Earlier delivery stages and assessments (historical)


<!-- bdg2-threefold-20260913: historical content -->

# ConfoSense current panel response matrix

**Amendment-004 status update:** two operational folds, reserved final calibration, confidence-bound selection/abstention, causal method-preserving replay, clean workload, shared-fit/independent comparisons and paired group/time inference interfaces are implemented and tested. The four-task no-fit audit and **SMOKE ONLY** integration are complete; final regression is **332 passed, 1 skipped, three existing warnings**. See [the implementation report](AMENDMENT004_IMPLEMENTATION_REPORT.md) for exact evidence and limits. PLEIA temperature/energy and RICO lack inner event support; BDG2 alone passes the structural screen. Real operational effectiveness, broader inference and dissertation readiness remain unestablished. The earlier table below records the preceding pilot/diagnostic stage, rather than the latest implementation status.

Updated 13 September 2026 after the integration repairs, completed PLEIA pilot and bounded diagnostics. See the [current evidence index](review/CURRENT_EVIDENCE.md), [diagnostics](PILOT_DIAGNOSTICS.md) and [operational plan](OPERATIONAL_EVALUATION_PLAN.md). This table supersedes the implementation-status claims in the **archived assessment** below. See [MODEL_COMPARISON_PILOT_REPORT.md](MODEL_COMPARISON_PILOT_REPORT.md) for measured results and [INTEGRATION_REPAIR_REPORT.md](INTEGRATION_REPAIR_REPORT.md) for the repaired foundations.

| Panel topic | Completed evidence | Still required |
|---|---|---|
| Explain conformal prediction | Pilot implements and validates ordinary split conformal from each fitted model's own absolute calibration errors; exact finite-sample ranks and explicit insufficient-support handling. | Simple mechanism example in final slides; retain temporal-dependence caveat and distinguish output validity from nominal coverage. |
| Dataset sources/purpose/selection | Actual four-task membership audited; source data and cohort history preserved. PLEIA pilot uses the existing target/cohort; separate BDG2 preparation includes all ten retained buildings. | PLEIA source authors/license verified in review/DATA_NOTICE.md; finish the broader bibliography and source-purpose-selection table. |
| Separate datasets or merge | Separate task paths retained; pilot results concern PLEIA temperature only. | Clearly state three source datasets/four tasks; do not pool raw errors across physical units. |
| Justify methods | Correct model attribution and own-model 90%/95% split-conformal calibration demonstrated for persistence, XGBoost and Attention-LSTM. | Broader method evaluation/citations; explicit DSCP and seasonal scope. Pilot split conformal is not CQR, EnbPI or DSCP. |
| Explain train/calibration/test protocol | Repaired boundaries/scaling; frozen actual pilot endpoints and common support; final calibration reserved before two expanding purged model-tuning folds, equal inner-MAE weighting and deterministic ties. | Implement the now-specified amendment-004 two operational folds, keeping final calibration reserved; explain broader folds and prior test inspection. |
| Explain k-of-m rules | Physical grid/support repairs retained. Pilot deliberately has no alert-rule selection. | Implement the operational plan's confidence-bound feasibility, recall/workload, synthetic F1, censored delay, deterministic ties and matched-budget endpoints before core fitting. |
| Simplify presentation | Completed source-linked four-panel pilot figure, visually checked; report distinguishes pilot, historical and missing evidence. | Validated broader inference and concise revised slides/candidature report. Old CIs are not current slide values. |
| Verify complete methodology/ablation | Checkpointed pilot completed at **9 point / 18 interval cells**; exact recomputation and all-nine-unit zero-refit resume passed. Historical artifacts preserved. | Full model/horizon/level evaluation, core fault/clean-counterfactual endpoints, matched-budget ablation, 60-unit core and 900-cell robustness reruns, group-recovery inference. |
| Is LSTM's extra cost worthwhile? | Measured pilot point/interval metrics and separate tuning, fit, calibration, inference, throughput and memory. LSTM had greater cost and worse point errors/Winkler scores at every pilot horizon; no statistical-superiority claim. | Matched comparison across all 13 task/horizon combinations and multiple folds/seeds; group-appropriate uncertainty and task-specific resource measurements. No requirement that LSTM win. |

**Pilot completion:** completion run exit **0**; **all 9/9 point and 18/18 interval cells passed output validation**, with no missing/duplicate/failed cells. The interrupted original launch has unknown exit status; its five completed checkpoints were preserved and reused. A complete-run resume verified all nine units without fitting. BDG2 preparation returned exit 0 with zero fitted models. Full preflight: **305 passed, 1 skipped, 3 existing MAPIE warnings**. This is bounded empirical evidence, not full-study/publication readiness.

**Bounded follow-up:** all 24 unchanged learned inner fits reproduced; six persistence supports are matched. No forecasting alignment/scaling/epoch defect was demonstrated. Preparation-only peak RAM fell 52.1% with identical support. Follow-up suite: **308 passed, 1 skipped, 3 existing warnings**; pilot revalidation: **9/9 point and 18/18 interval cells**.

**Next concrete proposal:** one BDG2 fold-2/seed-42 reduced operational pilot with five frozen catalogue seeds, as specified in the amendment report; not yet launched. DSCP/seasonal evaluation, real-data group-recovery inference and the full comparison remain in scope. The 207 retained RICO runs are retained data, not 207 pristine test runs.

---

## Archived initial panel assessment and context

# ConfoSense panel response matrix

13 September 2026. Based on the nine feedback topics in the supplied recovery document; the original panel sheet and current dissertation manuscript were not provided. This maps evidence and gaps, rather than certifying manuscript revisions. See [recovery status](PROJECT_RECOVERY_STATUS.md) for Git, artifact integrity, execution status and newly identified code discrepancies.

Paths are relative to `smart_building_conformal/`; `F` abbreviates `outputs/final_dissertation_v2/`. Priorities: P0 before another fit or final scientific claim; P1 substantive evidence gap; P2 explanation/presentation work. No suggested experiment or editing task below has been executed.

| Panel comment | Required response | Existing evidence/file | Gap | Next action |
|---|---|---|---|---|
| Explain conformal prediction simply and why it matters | Describe a prediction range calibrated using separate observations, then how repeated range violations inform monitoring. Distinguish prediction intervals from confidence intervals for summary metrics. | `src/conformal_cqr.py`; `src/alerts_corrected.py`; `outputs/figures/figure_3_interval_alert_mechanism.png`; `F/report/CLAIMS_DISPOSITION.md` | Existing prose is technical; blanket achieved distribution-free coverage is withdrawn. | **P2:** Use the short explanation below and one illustrative example. Pair empirical coverage with width; avoid a real-fault probability interpretation. |
| Explain dataset sources, original purposes and selection | Identify the original source, domain/purpose, chosen target, sampling interval, inclusion rules and role in this study. | `outputs/full_study/manifests/dataset_sources.json`; adapters in `src/datasets/`; `F/metrics/FULL_RUN_INVENTORY.csv`; RICO sensor/run audits; BDG2 subset selection; PLEIA target selection | Evidence exists across files; the PLEIA source manifest and literature matrix use inconsistent author strings. No single verified dissertation source table. | **P2:** Consolidate the dataset table below; verify complete bibliographic identities and licences from original records. Distinguish downloaded corpus, retained data and outer-test exposure. |
| Evaluate separately or merge datasets? | State that independent models/evaluations are run by dataset/target; explain units, frequencies and regimes. | `src/corrected_study.py:709`; per-dataset core directories; `FULL_RUN_INVENTORY.csv`; `F/report/FINAL_DISSERTATION_RESULTS.md` | Combined CSVs and pooled exploratory ablation could look like pooled training. PLEIA temperature/energy are two targets from one source, not independent new datasets. | **P2:** Explicitly state “three source datasets, four evaluation tasks.” Do not merge raw streams or average raw temperature/energy errors; retain per-task results. |
| Cite and justify every method | Link each methodological choice to a primary source or identify it as a declared project design choice. | `outputs/full_study/combined/literature_benchmark_matrix.csv`; `report/benchmarking_and_contribution_wording.md`; method modules; frozen protocol | Twelve contextual entries are not a complete bibliography: several lack exact titles/DOIs. Architecture-specific attention source and engineering choices are not fully justified. None of the 12 was classified directly comparable. | **P2:** Build a method-to-source map using the inventory below, verify exact paper/version, explain adaptations. Do not claim numerical superiority over unrelated published datasets. |
| Justify original 60/20/20 and actual evaluation protocol | Explain historical allocation, chronology, calibration purpose and why the corrected design differs; give actual folds and test exposure. | `configs/study_full.yaml:28`; `F/protocol/evaluation_design.md`; Amendments 001–003; `F/metrics/FOLD_MEMBERSHIP.csv`; `src/corrected_study.py:90–182,561–584` | No evidence that 60/20/20 was empirically optimal. Current engine is not simply that split; nested/refit cuts do not fully enforce the protocol's boundary promises. | **P0/P2:** Validate boundaries first. Explain historical 60% fitting / 20% calibration / 20% assessment as an allocation choice, not a conformal requirement or proven optimum. Report actual outer/inner design and disclosure of prior test inspection. |
| Explain and justify leakage-safe k-of-m rules | Explain k violations among the last m readings; physical duration, group resets, episode counting and inner-only selection under a frozen workload/recall policy. | `src/alerts_corrected.py`; `src/corrected_study.py:385–393,458–555`; `tests/test_alerts_corrected.py`; frozen rules and selection policy | Primitive safety is implemented; nested temporal boundaries need validation. Engine omits the declared 360-minute candidate. No rule is universally optimal; matched-budget comparison was not done. | **P0/P2:** Validate actual selection inputs and reconcile candidate list. Present one illustrative rule, record inapplicable rules and abstentions, and report outer generalisation separately from inner feasibility. |
| Make the presentation simpler | Show the mechanism, protocol, central results and limitations in a small number of readable visuals/tables. | Legacy methodology figures; four core figures; v2 generators; `F/report/FINAL_DISSERTATION_RESULTS.md` | Ten-figure v2 set is absent; old coverage figure uses superseded CIs. Efficiency/achievement prose contains smoke or nonexistent evidence. | **P0/P2:** Correct report evidence gating, then regenerate only necessary figures from audited CSVs. Use the short presentation sequence below; detailed grids belong in appendices. |
| Verify the complete methodology with baselines and ablations | Show exactly which components execute together and compare controlled variants on matched support. | Core 240 ablation rows; `PAIRED_ABLATION_EFFECTS.csv`; `src/corrected_study.py:645–687`; Amendment 002 | Four alert levels execute, but chosen point model does not drive their intervals; LSTM/seasonal-naive absent from corrected point selection, DSCP not executed, and full quality grid absent. Periodic residual input is problematic. Frozen matched-budget endpoint absent. | **P0/P1:** Complete the no-fitting integration audit in the recovery report. Retain existing contrasts as exploratory; propose only the minimum validated comparisons needed for the original methodology. |
| Does Attention-LSTM's computation buy better intervals than XGBoost? | Compare empirical coverage, MPIW, Winkler, MAE/RMSE, fitting/inference time and memory on identical targets, horizons, levels and evaluation support, with paired uncertainty. | Historical `outputs/full_study/combined/point_metrics.csv` and `interval_metrics.csv`; model modules; `F/metrics/POINT_EFFICIENCY_EVIDENCE_AUDIT.csv`; extension source | **No fair corrected comparison exists.** Historical CQR uses HistGBR; corrected core excludes LSTM and saves neither bounds nor outer point metrics. Extension only fits persistence/XGBoost point models and times interval estimators. | **P1, after P0 audit:** Use the minimal comparison specification below. Report a neutral trade-off, including no benefit or negative findings; do not require LSTM to win. |

## Short explanation for the panel

A point forecast gives one expected sensor reading. Conformal prediction uses prediction errors or conformity scores from separate calibration data to construct a range around a forecast. The range communicates uncertainty: a reading outside it can prompt attention, and repeated violations can trigger an alert. A nominal 95% interval expresses a target for long-run coverage under the method's assumptions; it is not a 95% probability that a detected event is a real fault. Temporal dependence and changing building conditions require empirical checking. This explanation follows the [conformal prediction tutorial by Angelopoulos and Bates](https://arxiv.org/abs/2107.07511).

Illustrative only: a temperature forecast of 24°C with a 22–26°C interval gives the operator more information than 24°C alone. Three violations among the last five observations could trigger a persistence rule. With ten-minute sampling, five observations represent a 50-minute monitoring window; the actual experiment uses its declared physical-time candidates, not this illustrative rule by default. Extremely wide intervals can cover almost everything while being poor monitors. Coverage, width, missed synthetic events and alert workload must therefore be read together.

In this project, the defensible motivation is **uncertainty-aware monitoring under imperfect data**, with observed trade-offs and limitations. Improved energy use, measured SDG impact and real fault precision were not established.

## Dataset evidence to consolidate

| Source / original purpose | Selected task and reason | Current evidence and limitation |
|---|---|---|
| **PLEIAData**, public smart-building consumption/HVAC/environment/motion data. Local provenance points to [Zenodo record 7620136](https://zenodo.org/records/7620136). | Indoor temperature, block B room 11 V2; and block B interval consumption `dif_cons`, both ten-minute sampling. Two physical targets test different dynamics. Interval increments avoid making cumulative-energy persistence trivially accurate. | 50,543 temperature and 50,545 energy observations; one series each. Local source manifest, target-selection records and energy profile document choices. Direct source-page retrieval failed during this assessment; original-purpose wording and full author citation still need primary-record verification. |
| **RICO**, controlled HVAC/test-cell indoor and outdoor sensor measurements. The [authors' dataset record](https://zenodo.org/records/14871584) describes five acquisition phases and four-hour multivariate series. | B.RTD3 air temperature, one-minute sampling; documented indoor measurement location. Use author quality flags and preserve experimental-run boundaries. | 207 retained runs; 49,680 observations. Only three runs were outer tests. Published corpus size and retained-run count differ for documented preprocessing reasons. Retained non-test runs were also used for fitting/calibration. |
| **BDG2**, energy-meter and weather/metadata benchmark; part supported ASHRAE GEPIII prediction and measurement/verification. [Authors' release](https://zenodo.org/records/3887306). | Hourly electricity for ten buildings chosen by documented quality criteria, providing a multi-building energy task. | 175,440 observations; weather recorded as loaded. Evaluation is temporal within known buildings, not unseen-building portability. Avoid translating within-building evidence into a new-building claim. |

These are separate evaluation tasks. Combining their summary tables is for reporting; it does not turn their heterogeneous streams into a single training dataset. Auxiliary UCI material is historical portability context and is not part of the corrected four-task core; no dataset expansion is proposed.

## Method rationale and citation inventory

| Method/choice | Rationale and source status | Current execution status |
|---|---|---|
| Persistence / seasonal naive | Transparent no-learning baseline; daily seasonality is applicable only when sufficient history exists. State the definition and source used in the dissertation. | Persistence scored in corrected inner point selection; seasonal naive exists in the older pipeline, absent from corrected engine. |
| XGBoost | Nonlinear predictor for tabular lagged/covariate inputs; [Chen and Guestrin, XGBoost](https://arxiv.org/abs/1603.02754) is a verified primary source. | Executed for inner point scoring; separately used as preferred EnbPI base estimator. Those are different fits. |
| Attention-LSTM | Sequential predictor with learned attention over temporal representations; plausible comparator, not an assumed improvement. Cite LSTM foundations and the exact attention architecture/adaptation, rather than a generic “attention” citation. | Implemented in `attention_lstm.py` and executed historically in `study_runner.py`; absent from corrected engine and extension. |
| Uncalibrated quantiles / CQR | Isolate the value of conformal calibration around the same quantile estimators. Primary method: [Romano, Patterson and Candès, CQR](https://papers.nips.cc/paper_files/paper/2019/hash/5103c3584b063c431bd1268e9b5e76fb-Abstract.html). | HistGradientBoostingRegressor lower/upper/median quantiles via MAPIE; both arms execute. Not LSTM intervals. |
| Static / updated recentred EnbPI | Ensemble time-series interval baseline; [Xu and Xie, ICML 2021](https://proceedings.mlr.press/v139/xu21h.html) is a verified primary source. The local matrix says 2023: identify whether it intends a later version. Explain the project-specific recentering and group-safe update adaptation. | Both interval candidates execute. Preferred base is XGBoost, with RandomForest fallback in code; corrected artifacts do not retain the actual fallback/base-estimator field. |
| DSCP | Multi-step dependence-aware interval method; [Dual-Splitting Conformal Prediction](https://arxiv.org/abs/2503.21251) provides the method source. Explain differences from the paper. | Implemented historically; explicitly not applicable in corrected single-horizon alert operation. No corrected multi-horizon DSCP results. |
| Static/periodic/rolling recalibration | Compare adaptation to evolving residual distributions; document construction, timing, residual delay and project-specific settings. Exact literature mapping remains incomplete. | Candidates execute, but core additional recalibration receives zero test residuals; validate before claiming the intended adaptation was evaluated. |
| k-of-m, episodes, tolerance, workload/recall policy | Engineering choice to control alert persistence/workload. Separate a literature motivation from the prospectively declared numerical settings; cite the protocol for the latter. | Group-safe primitives and episode evaluation execute; candidate and boundary discrepancies remain. |
| Bootstrap, paired effects, Holm; coverage/MPIW/Winkler/MAE/RMSE | State each estimand, independent unit, score definition and multiplicity family, with foundational references in the final bibliography. No raw-error averaging across target units. | Corrected core CIs/paired effects exist with limitations; extension inference code exists without full input results. Other historical tests such as DM/rank tests do not validate the corrected pipeline. |

The existing 12-row literature matrix is a useful starting inventory, not proof that all citations are accurate or that every implementation follows its cited paper. A bibliography verification task can be completed without fitting models.

## Attention-LSTM versus XGBoost: exact attribution and availability

The current call paths are:

| Path | What produces the saved interval or score? |
|---|---|
| Historical `study_runner._point_for_horizon` → `attention_lstm.train_predict` / `xgboost_model` | Model-specific point metrics; older diagnostic protocol. |
| Historical/corrected CQR → `conformal_cqr.fit_cqr` → `_quantile_estimator` | HistGradientBoostingRegressor quantiles; independently calibrated by MAPIE. |
| Corrected `_all_intervals` → `conformal_enbpi.run_enbpi` | Independently fitted EnbPI estimator, normally XGBoost, potentially fallback; not the point model chosen on inner MAE. |
| Corrected `evaluate_outer` → `_fit_interval_on` → optional `_recalibrated_stream` → `alert_on_stream` | Bounds from the interval method; the separately selected `point_model` is only recorded alongside them. |
| Extension `evaluate_unit` → `UnitModel` and separate `xgboost_model.tune` | Interval-method cell metrics plus an independent persistence/XGBoost point comparison; no LSTM. |

Thus interval metrics are **interval-estimator-specific**, not attributable to the independently selected point-model label. “Attention-LSTM + CQR” would be an unsupported attribution in these outputs.

| Requested comparison | Saved evidence | Fair corrected LSTM–XGBoost comparison? |
|---|---|---|
| Empirical coverage at matching horizon/nominal | Core has selected-method coverage at h=1 for PLEIA/energy/BDG2 and h=5 for RICO, operating levels 0.95–0.995. | No LSTM intervals; not a full matched 90/95% quality grid. |
| MPIW and Winkler | Older full-study interval table and extension smoke cells only; core bounds absent. | No. Cannot recover from aggregate coverage. |
| MAE / RMSE | Historical combined point table contains both models; corrected core does not save outer values. | No corrected matched model comparison. |
| Training time | Extension smoke interval-fit times; older mixed-stage timing/provenance may provide context. | No comparable per-model measurement; extension XGBoost `fit_seconds` also includes tuning and prediction. |
| Inference time | No saved clean, model-specific benchmark for the two models. | No. Needs separately measured inference. |
| Memory | Not instrumented. Existing LSTM runs on CPU. | No. Cannot reconstruct historical peak memory from metrics. |
| Paired uncertainty | Core ablation effects concern alert levels, not these two forecasters. | No. Need matched prediction errors/bounds and correct independent-unit handling. |

### What can be done from saved data, without fitting

The historical `combined/point_metrics.csv` can be summarized by matching dataset and horizon, explicitly as pre-repair diagnostics. Preliminary `outputs/predictions/point_predictions_h*.csv` and laptop-verification PLEIA prediction rows contain both models and allow common-row error comparisons. These are different, historical runs; first audit nonmissing common support and label their provenance.

Older full-study per-observation prediction directories are absent locally. The corrected core has no point predictions or interval bounds. Consequently, corrected LSTM–XGBoost coverage/width/Winkler and paired error CIs **cannot** be recovered by postprocessing the available core CSVs. Calibration residuals cannot be inferred from aggregate point errors. Do not use test residuals to manufacture missing calibration data.

### Smallest new-fitting comparison, only after validation and approval

Use the existing two model families and persistence context on the already declared datasets/splits; start with the already selected primary horizon per task, explicitly identifying this as a bounded comparison rather than completion of the whole multi-horizon grid. Use the existing methodological nominal levels, **90% and 95%**, matched across models. A broader horizon grid is required only if the final claims retain that scope.

Use a common, explicitly declared calibration construction around **each model's own predictions**. A residual-based construction already represented in this repository is a candidate; confirm its finite-sample quantile rule and temporal assumptions before adopting it for this comparison. This would be a reviewed protocol clarification/change, not permission to relabel HistGBR CQR as an LSTM method. If the dissertation specifically requires quantile-LSTM CQR, that requires additional quantile-model implementation and is not the minimal task.

Acceptance criteria for an authorised comparison:

- Same outer test timestamps/groups, horizons, information available at prediction origin, calibration scope, nominal levels and evaluation masks. Tune using permitted inner data only; fit scaling on permitted training data only.
- Persist per-row truth, prediction, lower/upper bounds, model identity, dataset/fold/seed/horizon/level, and calibration/provenance hashes. Explicitly report missing/inapplicable cells and execution errors.
- Compute coverage, MPIW, Winkler, MAE and RMSE on common support. Pair model differences within the same unit; aggregate algorithmic seeds before run/building/period inference. Retain NA CIs where independent support is insufficient rather than creating pseudo-replication.
- Time tuning, final fitting and inference separately under recorded hardware/thread/batch conditions; account for asynchronous device execution if relevant. Measure process peak memory and accelerator memory where actually used. Historical smoke times are not a baseline for this comparison.
- Assess both interval usefulness and costs. Wide intervals or absent coverage cannot be offset by a favourable point-error number. A conclusion that the extra LSTM cost provides no demonstrated benefit is acceptable.

## Simpler presentation sequence

1. Explain the forecast → calibrated interval → repeated violation → alert mechanism with an explicitly illustrative example.
2. Show three source datasets/four tasks, then actual split units, horizons and outer-test exposure.
3. Show feasibility first: 12/60 eligible units; distinguish inner selection, feasible-only outer results and all-unit diagnostics.
4. Present empirical coverage against each selected nominal, then paired exploratory ablations and the missing model-cost comparison. Avoid superseded figures.
5. End the substantive discussion with observed limitations: small independent sample, synthetic events, undercoverage, incomplete extension and unresolved integration checks. Put implementation grids and detailed audits in appendices.

Only this matrix and the recovery-status file were created. Changes to reports, code, protocols and experiments remain proposals for the user's review.
